메인 페이지(get, /)
  업체 조회(get, /company)
  비 로그인
    로그인(get, /member/login)
    회원가입(get, /member/join)
  로그인
    내 정보(get, /member/myinfo)
    로그아웃(post, /member/logout)
  로그인-권한 ADMIN
    관리자 페이지(get, /admin/main)
  로그인-권한 EVALUATOR
    평가자 페이지(get, /evaluator/main)

업체 조회(get, /company)
  업체 조회(get, /api/company)

회원가입 페이지(/member/join)
  아이디 중복확인(get, /api/member/check-id)
  닉네임 중복확인(get, /api/member/check-nickname)
  폼 제출(post, /member/join)

로그인 페이지(/member/login)
  폼 제출(post, /member/login)

관리자 페이지(/admin/main)
  업체 추가(get, /company/registor)
  평가 추가(get, /evaluation/registor)
  평가 검색(get, /evaluation)
  회원 검색(get, /member)

업체 추가(/company/registor)
  사업자등록번호 확인(get, /api/company/check-businessno)
  폼 제출(post, /company/registor)

업체(/company/{encCompanyNo})
  목록으로(get, /company)
  업체 로고(get, /file/company/{encCompanyNo}/logo)
네비 바
  기본 정보(get, /company/{encCompanyNo})
  업체 소개(get, /company/{encCompanyNo}/intro)
  평가 목록(get, /evaluation/{encCompanyNo})
  위치 확인(get, /company/{encCompanyNo}/location)
  작업 현황(get, /company/{encCompanyNo}/dashboard)
기본 정보
  로고 조회(get, /file/company/{encCompanyNo}/logo)
  권한 - ADMIN
    업체 로고 등록(post, /file/company/{encCompanyNo}/logo)
    업체 수정(put, /api/company/{encCompanyNo})
    업체 서류 등록(post, /file/company/{encCompanyNo}/doc)
    업체 서류 보기(get, /file/company/{encCompanyNo}/doc/{encDocNo})
    업체 서류 삭제(delete, /file/company/{encCompanyNo}/doc/{encDocNo})
    -실제 파일 삭제 로직 구현 필요해요!!
업체 소개
  사진 불러오기(get, /file/company/{encCompanyNo}/intro/{changedName})
  권한 - ADMIN
    소개 생성/수정(put, /api/company/{encCompanyNo}/intro)
    사진 삽입(post, /file/company/{encCompanyNo}/intro)
평가 목록(/evaluation/{encCompanyNo})
위치 확인(/company/{encCompanyNo}/location)
  권한 - ADMIN
    지오코드(get, /api/location/geocoding)
    위치 추가(post, /api/company/{encCompanyNo}/location)
    위치 삭제(delete, /api/company/{encCompanyNo}/location)
작업 현황(/company/{encCompanyNo}/management)
  권한 - ADMIN
    지오코드(get, /api/location/geocoding)
    현황 추가(post, /api/company/{encCompanyNo}/management)
    현황 수정(put, /api/company/{encCompanyNo}/management)

회원 검색(/member)
  회원 목록 불러오기(get, /api/member)
  회원 세부사항으로(get, /member/{encMemberNo})

회원 세부사항(/member/myinfo, /member/{encMemberNo})
  정보 수정(put, /api/member/{encMemberNo}/update)
  닉네임 중복확인-수정용(get, /api/member/check-updatednickname)
  회원 상태변경(put, /api/member/{encMemberNo}/update/status)
  회원 권한 변경(put, /api/member/{encMemberNo}/update/role)
  회원 탈퇴(post, /member/delete)